# 更新记录

## v2.1 — 2026-09-22

- 精简：下线页面编辑类功能（布局设计器 / 标签编辑器），只保留最基本的打印转换能力；相关组件原样归档到 `extras\`（未启用）。
- 新增「操作页面」：本地浏览器界面（http://127.0.0.1:8787/，仅本机可访问）——运行状态实时显示、虚拟打印机检测/修复、打印校准唛、目录快捷入口、最近处理与日志查看。
- 后台常驻：软件改为无窗口静默运行（单实例保护，重复启动自动忽略），并已安装「登录即启动」开机自启任务。
- 新增启动/停止入口：打开操作页面.cmd / 停止软件.cmd（替代旧的启动/停止监视与控制面板脚本）。
- 命令行精简：新增 console 命令；移除编辑类命令。
- 文档更新：《使用说明与交付报告》更新至 v2.1。
- 修复：打印机状态检查的 PowerShell 子进程改为完全隐藏窗口（此前每次检查会闪现蓝色控制台窗口；重启服务并实测验证）。
- 修复：鞋类「同一张唛连打两遍」只出一张——文件槽判重改为按「内容 + 写入时间」识别，每一笔真实写入（含内容相同的复打）都会被捕获打印；归档重复文件名自动加序号。
- 新增：归档 / 预览目录每日自动清理（默认保留最近 7 天，可在 config.json 的 cleanup 中调整；每天自动执行一次，另提供 cleanup 命令可手动执行）。
- 新增：Windows 一键安装包（Inno Setup）：内嵌独立运行环境（目标电脑无需预装任何软件）、安装目录可选、自动完成虚拟打印机/开机自启/配置初始化，支持完整卸载；附「卸载清理」安全脚本与可复现构建脚本（构建工具\安装包\）。

## v2.0 — 2026-09-22

- 架构升级：不再依赖第三方虚拟打印机（pdfFactory）——工具自建专属虚拟打印机「水洗唛打印助手」（系统内置 PDF 驱动 + 文件端口），打印任务直接进入工具完成转换与输出。
- 新增「文件槽」高速捕获（毫秒级轮询 + 完整性校验 + 哈希去重），实测瞬间连打 3×3 全部捕获、无丢失。
- 新增版面「截取对齐」（2.03mm 补偿，逐像素对齐旧版式）与捕获页安全校验。
- 新增「布局设计器」：自由设计水洗唛版面（文本/条码/线条、{字段}占位符、模板管理、双向预览、一键打印）；支持「用于自动打印」按自定义布局重排（识别失败自动回退）。
- 新增虚拟打印机一键安装/卸载脚本与状态查询；控制面板增加状态入口。
- 文档全面更新（使用说明与交付报告 v2.0）。

## v1.0 — 2026-09-22

- 初版发布：洗衣管家水洗唛「横版 → 竖版」全自动旋转打印（自动捕获 → 旋转 90° → 送印 → 归档）。
- 标签内容编辑器（初版）：识别打印数据字段 → 编辑 → 实时预览 → 直接打印 / 另存图片。
- 安全校验：非标签尺寸的 PDF 自动转存 `out\hold`，不会误打印。
- 交付文档：《使用说明与交付报告.docx》与《使用说明.html》。
