# 水洗唛打印助手 · 开发交接文档

> 版本：v2.1 + 一键安装包 · 日期：2026-09-22 · 适用：后续维护 / 二次开发 / 交接

## 1. 项目速览

- **一句话**：把洗衣管家打印的水洗唛（120×30mm 横版内容）自动转换为门店标签机（佳博 GP-9134T）的出纸规格（30×120mm 竖版）并完成打印与归档。
- **仓库**：<https://github.com/Jace-Hao/Print-Converter>
- **发布页（含安装包）**：<https://github.com/Jace-Hao/Print-Converter/releases/tag/v2.1>
- **当前状态**：v2.1 已在门店工作机常驻运行（后台服务 + 操作页面 + 开机自启）；Windows 一键安装包已发布。
- **本地位置**：开发副本 `E:\软件开发\水洗唛打印助手`；安装版可自选目录（安装包默认 `C:\水洗唛打印助手`）。

## 2. 系统架构

**数据流**：

```
洗衣管家（打印，选择「水洗唛打印助手」）
  → 自建虚拟打印机（Microsoft Print To PDF 驱动 + 文件端口 spool\capture.pdf）
  → 文件槽捕获（60ms 轮询）
  → 截取对齐（[0,0,120,30]mm + 2.03mm 补偿）
  → 旋转 90°（默认顺时针）→ 排版 30×120mm
  → 打印到「水洗唛」（GP-9134T, 300dpi 单色）
  → 归档 + 预览 + 日志
```

**组成**：一个后台进程（`app/console.py`，无窗口、单实例、开机自启）同时提供：① 监视处理管线；② 本地操作页面（<http://127.0.0.1:8787/>，仅本机）；③ 每日清理。

**三个关键机制**（重要，维护时理解这几条即可）：

1. **文件槽捕获**：虚拟打印机每次任务都会重写 `spool\capture.pdf`；服务以 60ms 轮询读取完整版本（%PDF/%%EOF 校验），判重规则为「**内容 + 写入时间都相同**才算同一版本」——保证同一张唛复打两遍（鞋类常见）也能逐张出纸（修复于 2026-09-22）。
2. **截取对齐**：捕获页为 A4，仅截取左上角 `[0,0,120,30]mm` 并按 `shift_mm=[2.03,0]` 平移，与旧版式逐像素对齐（偏差 ≤1px）。
3. **每日清理**：归档 / 预览默认保留最近 7 天（`config.json → cleanup`，每日自动执行，另支持 `cleanup` 命令手动执行）。

**模块表**（`app/`）：

| 模块 | 职责 |
|---|---|
| `config.py` | 配置加载/保存（含全部默认值） |
| `logutil.py` | 日志（`out\logs\YYYY-MM-DD.log`） |
| `render.py` | PDF → 位图：渲染、截取、旋转、排版 |
| `printutil.py` | GDI 直打（1:1 像素到标签机） |
| `pipeline.py` | 单文件处理：逐页转换 → 打印 → 归档 |
| `watcher.py` | 目录监视 + 槽文件处理 + 安全校验 |
| `capture.py` | 文件槽高速捕获（轮询 + 判重） |
| `console.py` | 后台服务：监视器 + 操作页面 + 每日清理线程 |
| `vpinstall.py` | 虚拟打印机安装/卸载/状态（管理员） |
| `retention.py` | 每日清理（归档/预览过期文件） |
| `cli.py` | 命令行入口（console/watch/once/cleanup/init 等） |

## 3. 目录与文件

| 路径 | 说明 |
|---|---|
| `打开操作页面.cmd` / `停止软件.cmd` | 日常入口：启动（后台静默）并打开操作页面 / 停止软件 |
| `安装虚拟打印机.cmd` / `卸载虚拟打印机.cmd` | 虚拟打印机安装 / 卸载（需管理员确认一次） |
| `安装开机自启.ps1` / `卸载开机自启.ps1` | 开机自启的安装与卸载（支持 `-Silent`） |
| `卸载清理.ps1` | 卸载清理：仅停止/移除**指向本目录**的服务、自启、虚拟打印机（安全限定） |
| `打印校准唛.cmd` / `把pdf拖到这里处理.cmd` | 打印两版校准唛 / 手动处理单个 PDF |
| `app\` | 程序代码 |
| `web\操作页面.html` | 操作页面（文案/配色可改，文件顶部有指引） |
| `构建工具\安装包\` | 安装包构建：Inno Setup 工程 + `build_installer.ps1` |
| `开发工具\验证脚本\` | 历史验证/回归脚本（7 个 + 说明，见该目录 README） |
| `docs_build\` | 《使用说明与交付报告.docx》生成脚本（备用） |
| `extras\` | 已下线组件存档（布局设计器等，原样保留，主链路零引用） |
| `out\archive\` / `out\previews\` / `out\logs\` | 归档 PDF / 位图预览 / 日志（均参与每日清理，日志除外） |
| `spool\` | 虚拟打印机捕获文件与作业暂存（运行数据） |
| `dist\` | 安装包产物（构建输出） |
| `backup\` | 本机状态备份（含找回的历史文件副本，不入库） |
| `config.json` | 运行配置（见 §4） |

## 4. 运行与维护

**日常操作**：双击「打开操作页面.cmd」→ 浏览器打开操作页面（状态、虚拟打印机检测/修复、打印校准唛、打开目录、最近处理、日志）。

**config.json 关键项**：

| 项 | 默认 | 说明 |
|---|---|---|
| `pipeline.rotate_dir` | `cw` | 旋转方向（顺时针/逆时针，待现场目检确认） |
| `capture.crop_mm` / `shift_mm` | `[0,0,120,30]` / `[2.03,0]` | 截取与对齐补偿（校准用） |
| `print.printer` | `水洗唛` | 目标标签机 |
| `cleanup.days` | `7` | 归档/预览保留天数（0 = 每次清理时清空） |
| `skip_print.keywords` | 布草 / 洗涤服务 / 窗帘 / 窗纱 | 命中关键词的标签不打印（仅保留归档与预览） |
| `automation.mode` | `auto` | 改 `dry_run` 则只预览不打印（调试用） |
| `watch.dirs` | 见文件 | 兜底监视目录（旧 pdfFactory 自动保存等） |

**常用命令**（在项目目录运行）：

```
python -m app.cli console          # 启动后台服务 + 操作页面（主进程）
python -m app.cli cleanup          # 立即清理归档/预览过期文件
python -m app.cli init             # 安装后初始化（规范化配置路径）
python -m app.cli calibrate        # 打印两版校准唛
python -m app.cli vp status        # 虚拟打印机状态
python -m app.cli printers         # 打印机与分辨率信息
python -m app.cli info 文件.pdf    # PDF 页面几何信息
```

**故障排查速查**：

| 现象 | 处理 |
|---|---|
| 打印后没出标签 | 检查打印时选的打印机是否为「水洗唛打印助手」；打开操作页面看状态；查 `out\logs` 当天日志 |
| 打印机不见了 | 双击「安装虚拟打印机.cmd」重装；或操作页面 →「虚拟打印机」检测/修复 |
| 页面打不开 | 双击「打开操作页面.cmd」（会确保服务在运行）；如端口被占用会自动尝试相邻端口 |
| 某类打印没自动出 | 内容不在标签区域的文件会被安全闸移入 `out\hold`（防止误打），人工确认后处理 |
| 标签方向不对 | 改 `pipeline.rotate_dir` 后重启软件（先打校准唛对比） |

## 5. 构建与发布（一键安装包）

- **构建**：`构建工具\安装包\build_installer.ps1`
  - 前置：Inno Setup 6（默认路径 `C:\Program Files (x86)\Inno Setup 6\ISCC.exe`）；**首次构建需要网络**（自动下载嵌入式 Python 运行时并安装依赖）。
  - 参数：`-RuntimeFrom <已有运行时目录>`（复用，跳过下载）；`-SkipRuntime`（要求 `.build\payload\runtime` 已存在）。
  - 流程：组装 payload（程序 + 内嵌运行时）→ 规范化 config → ISCC 编译 → 产物在 `dist\`。
- **当前安装包**：`水洗唛打印助手-安装包-v2.1.exe`，SHA256 `12C683E3…C1ABAAB`（完整值见《安装部署说明》）。
- **发布**：GitHub Release 上传（tag `v2.1`）。注意：**发布平台资源名不支持中文**，线上资源名为 `Print-Converter-Setup-v2.1.exe`。
- **构建踩坑沉淀**（重构时注意）：
  1. `.cmd` 文件必须 **CRLF 行尾**（LF + 中文 + `chcp` 会被 cmd 解析出杂音命令）；
  2. `.ps1` 文件需 **UTF-8 BOM**（否则 PowerShell 5.1 中文乱码/报错）；
  3. 内嵌运行时 `python314._pth` 需包含 `..`，否则 `app` 包无法导入；
  4. 卸载清理的进程匹配必须先限定 `Name = python*`（防止误杀 shell）；
  5. 静默测试安装：`安装包.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /NOICONS /DIR="<目标>" /TASKS="" /LOG="<日志>"`。

## 6. 版本历史

| 版本 | 日期 | 要点 |
|---|---|---|
| v1.0 | 2026-09-22 | 初版：捕获 → 旋转 → 打印；标签内容编辑器 |
| v2.0 | 2026-09-22 | 自建虚拟打印机 + 文件槽高速捕获；布局设计器（v2.1 已下线） |
| v2.1 | 2026-09-22 | 精简（编辑类下线归档）；操作页面；后台常驻 + 开机自启；每日清理；双张/闪窗等修复 |
| v2.1 安装包 | 2026-09-22 | Inno 一键安装：内嵌运行时、目录可选、完整卸载 |

## 7. 已知限制与注意事项

- **旋转方向**：出厂默认顺时针（`cw`），需现场目检两张校准唛后最终确认（改配置即可，不涉及代码）。
- **单实例**：同机只允许一个服务实例（端口 8787 起探测）；多份安装共用同一虚拟打印机与自启项（会指向最新安装的目录）。
- **捕获为覆盖式写入**：60ms 轮询已实测连打不丢失；极端情况可按 `out\archive` 记录补打。
- **旧通道兜底**：pdfFactory 自动保存目录仍被监听（兼容历史流程）。
- **卸载**：会移除程序、自启与虚拟打印机；`out\` 数据目录保留（如需彻底清理手动删除）。
- **安装包未做代码签名**：新机器首次运行可能有 SmartScreen 提示（选择“仍要运行”）。
- **非本项目提示**：计划任务「XQY-XYGJ-Bridge」为照片同步项目遗留（指向已不存在的脚本），与本工具无关，请勿改动或删除，待照片项目处理。

## 8. 交接检查清单（建议接手第一步）

- [ ] 双击「打开操作页面.cmd」，确认服务与虚拟打印机状态正常；
- [ ] 打印一张真实水洗唛，核对方向与位置（与旧版式一致）；
- [ ] 阅读《使用说明.html》《安装部署说明.html》；
- [ ] 确认可访问 GitHub 仓库与 Release 页；
- [ ] 了解安装包重建方式（见 §5）。

## 9. 记录位置

- 仓库文档：使用说明 / 安装部署说明 / 双张打印修复说明 / 本交接文档；
- 开发过程记录：`E:\软件开发\.cluster\wash-label\`（研究、计划、样例）；`E:\软件开发\memory\`（每日记录）；
- 验证脚本：`开发工具\验证脚本\`（README 说明各脚本用途与注意事项）。

## 附录 A：整理记录（2026-09-22）

- 删除构建缓存约 0.5GB（项目 `.build\`、临时区 `pkg\`，均为可重建产物）；
- 临时区其余 170 项 + 3 个重复安装包副本移入回收站（可恢复）；临时区已清空；
- 验证脚本 8 件归档至 `开发工具\验证脚本\`；历史找回副本移至 `backup\recovered-20260922\`；
- 其他项目目录未改动（洗衣管家上传助手、照片系统、图标/UI 改版、会员导出等）。
